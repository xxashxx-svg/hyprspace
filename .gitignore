# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# the marketing site is its own vite app
website/node_modules
website/dist

# local env — .env.example is the tracked template
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# updater signing keys — NEVER commit (they live in ~/.hyprspace-signing)
*.key
*.key.pub

# local release build artifacts
release-artifacts/

# intermediate icon raster (regenerate with: node scripts/gen-icon.mjs)
scripts/logo-1024.png
.playwright-mcp/

# tsc -b build info
*.tsbuildinfo

# maintainer-only tooling & internal docs — kept local, never published
deploy.ps1
mac-publish.ps1
docs/DEPLOY.md
docs/ENTITLEMENT.md
docs/audit/
scripts/gen-license-keypair.mjs
scripts/license-keygen.mjs
